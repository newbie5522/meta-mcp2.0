# Inactive Prisma Artifacts

This directory is inactive.

- `src/prisma/schema.prisma` is not used by package scripts.
- `src/prisma/schema.prisma` is not used by the runtime Prisma Client.
- `src/prisma/dev.db` is an inactive historical database artifact.
- Do not edit files in this directory for application schema changes.
- The canonical schema is `prisma/schema.prisma`.
- The active local SQLite database is `prisma/dev.db`.

